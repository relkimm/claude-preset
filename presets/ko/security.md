보안 취약점을 찾아서 보고합니다.

## 빠른 스캔

**하드코딩된 비밀**
- API 키, 비밀번호, 토큰 검색
- 확인: `.env` 파일, 설정 파일, 코드
- 패턴: `password`, `secret`, `api_key`, `token`

**취약한 의존성**
- `npm audit` (JavaScript)
- `pip-audit` (Python)
- 알려진 CVE 확인

## 상세 스캔

**인젝션 취약점**
- SQL 인젝션 (문자열 연결 쿼리)
- XSS (사용자 입력 직접 렌더링)
- Command 인젝션
- Path Traversal

**인증/권한**
- 평문 비밀번호
- 약한 세션 관리
- Rate limiting 없음
- 권한 체크 누락

**데이터 노출**
- 로그에 민감 데이터
- 상세한 에러 메시지
- 디버그 모드 활성화

## 결과 보고

```
치명적 (즉시 수정):
파일:라인 - [문제]
위험: [영향]
수정: [해결 방법]

경고 (빠른 수정):
[중요도 중간 문제들]

권장사항:
[보안 개선 제안]
```

## 우선순위
1. 악용 난이도 (쉬운가?)
2. 영향도 (피해가 큰가?)
3. 수정 난이도 (빠른가?)

사용: `/security`
